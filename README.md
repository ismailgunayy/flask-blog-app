# Flask Blog App

### Setting up the environment

```
pip install Flask
pip install Flask-MySQLdb
pip install Flask-WTF
pip install passlib
pip install wtforms
```

### Run the server

```
python app.py
```

Go to localhost:5000/
